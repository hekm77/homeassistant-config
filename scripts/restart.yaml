# ------------------------------------------------------------------------------
# - Перезагрузка Home Assistant
# ------------------------------------------------------------------------------
ha_restart:
  sequence:
    - service: shell_command.ha_restart

# ------------------------------------------------------------------------------
# - Перезагрузка / выключение Raspberry Pi
# ------------------------------------------------------------------------------
reboot_pi:
  sequence:
    - service: shell_command.reboot_pi

shutdown_pi:
  sequence:
    - service: shell_command.shutdown_pi
